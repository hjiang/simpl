(defn ->-build [v forms]
  (if (empty? forms)
    v
    (let [form (head forms)]
      (let [new-form (cons (head form) (cons v (tail form)))]
        (->-build new-form (tail forms))))))

(defmacro -> [v & forms]
  (->-build v forms))
